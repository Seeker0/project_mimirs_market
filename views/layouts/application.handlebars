<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>
      {{appName}}
      {{#if title}}
       | {{title}}
       {{/if}}
    </title>

    <!-- JQuery -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.1.1/jquery.min.js"></script>

    <!-- Bootstrap -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">


    <!-- Latest compiled and minified JavaScript -->
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js" integrity="sha384-Tc5IQib027qvyjSMfHjOMaLkfuWVxZxUPnCJA7l2mCWNIpG9mGCD8wGNIcPD7Txa" crossorigin="anonymous"></script>


    <script>
      var updateAll = function() {
        console.log("invoked")
        let results = {};
        var items = document.getElementsByClassName("cartItems");
        console.log(items)
        console.log(Object.keys(items))
        Object.keys(items).forEach(key => {
          let id = items[key].children["0"].innerHTML;
          let quantity = items[key].children["2"].valueAsNumber;

          results[id] = quantity;
        })
        console.log(results)
        document.getElementById("allItems").setAttribute("value", JSON.stringify(results));
        console.log(document.getElementById("allItems"))

        document.getElementById("submitAll").click();
      }
    </script>
    <!-- My Styles -->
    <link rel="stylesheet" href="/styles/stylesheets/css/styles.css" />
  </head>
  <body>
    {{> shared/_nav}}
    {{> shared/_flash}}
    <main class="container-fluid">
      {{{body}}}
    </main>
  </body>
</html>
